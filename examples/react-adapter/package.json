{
  "name": "breeze-react-adapter-example",
  "private": true,
  "version": "1.0.0",
  "description": "Genuine interop example: a real Breeze custom element (Breeze.defineElement) mounted inside a real Vite + React app. Not a rewrite of Breeze in React, and not a rewrite of React in Breeze.",
  "type": "module",
  "scripts": {
    "predev": "node scripts/copy-breeze.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-breeze.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/copy-breeze.mjs && node verify.mjs"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "jsdom": "^30.0.1",
    "vite": "^6.4.3"
  }
}
