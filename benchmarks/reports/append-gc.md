# 📊 Benchmark Report: Bulk Append Trace Breakdown (GC / Script / Layout)

> **Auto-generated by its runner script — do not hand-edit.** Regenerate with
> the command below; the numbers here are exactly what that command printed.

CDP trace breakdown of what happens during a 1,000-row bulk append (mounting 1,000 rows, then appending 1,000 more) in Breeze, 5 traced runs, phase totals meaned across runs. Run with `--react` to add a React 19 comparison column.

---

## Environment

- **Date**: 2026-09-13T06:31:00.971Z
- **OS**: Linux 6.8.0-101-generic (x64)
- **CPU**: AMD EPYC 9254 24-Core Processor — 48 logical cores
- **RAM**: 377.4 GiB
- **Node.js**: v22.23.1 (V8 12.4.254.21-node.56)
- **Git commit**: 4e9a54d (dirty working tree)
- **Environment**: ⚠️  Likely virtualized/shared host (systemd-detect-virt: docker) — expect more run-to-run noise than a dedicated desktop/laptop.

---

## Exact Reproduction Command

```bash
node benchmarks/append-profile-runner.js --runs=5
```

---

## Results (mean of 5 traced runs)

| Phase | Breeze |
| :--- | :---: |
| **Sync click-handler (median)** | 8.7 ms |
| **GC total (per append)** | **4.29 ms** (6/run) |
| Layout | 35.8 ms |
| UpdateLayoutTree | 18.5 ms |
| EventDispatch | 11.56 ms |
| PrePaint | 9.6 ms |
| ParseHTML | 8.16 ms |
| Paint | 3.31 ms |
| TimerFire | 0.23 ms |
| HitTest | 0.16 ms |
| FunctionCall | 0.11 ms |
| Layerize | 0.09 ms |

## Reading these numbers

- This report reflects the current codebase only — it is a snapshot, not a before/after comparison against any prior Breeze version. To see whether a change helped or hurt, run this script on two commits and diff the outputs yourself.
- "GC total" counts every trace event matching a GC-related name/category during the append; it is not exclusively attributable to Breeze's own allocations (V8's GC scheduling is influenced by overall heap pressure from the whole page).
- Phase names come directly from Chrome's `devtools.timeline`/`v8.execute` trace categories and are Chrome/V8-version-dependent — do not assume phase names are stable across Chrome versions.

---

*Generated 2026-09-13T06:31:00.971Z at git commit `4e9a54d` (dirty working tree — re-run after committing for a citable result).*
