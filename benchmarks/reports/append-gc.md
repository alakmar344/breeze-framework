# 📊 Benchmark Report: Bulk Append Trace Breakdown (GC / Script / Layout)

> **Auto-generated by its runner script — do not hand-edit.** Regenerate with
> the command below; the numbers here are exactly what that command printed.

CDP trace breakdown of what happens during a 1,000-row bulk append (mounting 1,000 rows, then appending 1,000 more) in Breeze, 2 traced runs, phase totals meaned across runs. Run with `--react` to add a React 19 comparison column.

---

## Environment

- **Date**: 2026-09-16T15:08:06.162Z
- **OS**: Linux 7.0.0-31-generic (x64)
- **CPU**: Intel(R) Pentium(R) CPU  N3700  @ 1.60GHz — 4 logical cores
- **RAM**: 3.7 GiB
- **Power**: Battery (47%, Charging)
- **Node.js**: v22.23.2 (V8 12.4.254.21-node.56)
- **Git commit**: 98dbd1c (dirty working tree)
- **Environment**: Virtualization status could not be determined on this OS.

---

## Exact Reproduction Command

```bash
node benchmarks/append-profile-runner.js --runs=2
```

---

## Results (mean of 2 traced runs)

| Phase | Breeze |
| :--- | :---: |
| **Sync click-handler (median)** | 79.4 ms |
| **GC total (per append)** | **121.22 ms** (168/run) |
| Layout | 314.58 ms |
| UpdateLayoutTree | 83.16 ms |
| EventDispatch | 71.48 ms |
| ParseHTML | 54.39 ms |
| PrePaint | 35.38 ms |
| Paint | 18.42 ms |
| HitTest | 4.11 ms |
| TimerFire | 1.79 ms |
| RunMicrotasks | 1.41 ms |
| Layerize | 0.69 ms |

## Reading these numbers

- This report reflects the current codebase only — it is a snapshot, not a before/after comparison against any prior Breeze version. To see whether a change helped or hurt, run this script on two commits and diff the outputs yourself.
- "GC total" counts every trace event matching a GC-related name/category during the append; it is not exclusively attributable to Breeze's own allocations (V8's GC scheduling is influenced by overall heap pressure from the whole page).
- Phase names come directly from Chrome's `devtools.timeline`/`v8.execute` trace categories and are Chrome/V8-version-dependent — do not assume phase names are stable across Chrome versions.

---

*Generated 2026-09-16T15:08:06.162Z at git commit `98dbd1c` (dirty working tree — re-run after committing for a citable result).*
